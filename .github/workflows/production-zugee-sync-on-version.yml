name: Sync Production Zugee on Version Tag Change

on:
  push:
    branches: [ "main" ]
    paths:
      - src/bot/zugee.js

permissions:
  contents: write

jobs:
  sync_production_zugee:
    name: Merge main into zugee-production
    runs-on: ubuntu-latest

    steps:
      - name: Checkout main
        uses: actions/checkout@v4
        with:
          ref: main
          fetch-depth: 0

      - name: Check Zugee version tag change
        id: version_tag
        shell: bash
        run: |
          set -e
          file="src/bot/zugee.js"
          current=$(head -n 1 "$file")
          echo "current=$current" >> "$GITHUB_OUTPUT"

          if [ "${{ github.event.before }}" = "0000000000000000000000000000000000000000" ]; then
            echo "previous=none" >> "$GITHUB_OUTPUT"
            echo "changed=true" >> "$GITHUB_OUTPUT"
            exit 0
          fi

          previous=$(git show ${{ github.event.before }}:$file 2>/dev/null | head -n 1 || true)
          if [ -z "$previous" ]; then
            echo "previous=missing" >> "$GITHUB_OUTPUT"
            echo "changed=true" >> "$GITHUB_OUTPUT"
            exit 0
          fi

          echo "previous=$previous" >> "$GITHUB_OUTPUT"
          if [ "$current" != "$previous" ]; then
            echo "changed=true" >> "$GITHUB_OUTPUT"
          else
            echo "changed=false" >> "$GITHUB_OUTPUT"
          fi

      - name: Setup Git config
        if: ${{ steps.version_tag.outputs.changed == 'true' }}
        run: |
          git config user.name "GitHub Actions"
          git config user.email "actions@github.com"

      - name: Pull latest zugee-production
        if: ${{ steps.version_tag.outputs.changed == 'true' }}
        run: |
          git fetch origin zugee-production || echo "zugee-production does not exist yet"

      - name: Merge main into zugee-production
        if: ${{ steps.version_tag.outputs.changed == 'true' }}
        run: |
          git checkout zugee-production || git checkout -b zugee-production
          if git merge main --no-ff -m "Auto deploy main -> zugee-production"; then
            echo "Normal merge completed."
          else
            echo "Retrying merge with unrelated histories..."
            git merge main --no-ff --allow-unrelated-histories -m "Initial zugee-production merge"
          fi

      - name: Push to zugee-production
        if: ${{ steps.version_tag.outputs.changed == 'true' }}
        env:
          TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          git push https://x-access-token:${TOKEN}@github.com/${{ github.repository }} zugee-production
